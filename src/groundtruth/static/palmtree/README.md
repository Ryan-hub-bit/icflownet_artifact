# `src/groundtruth/static/palmtree`

Bundled PalmTree model assets used to embed basic-block instruction text.

- `transformer.ep19`: pretrained PalmTree checkpoint loaded by the static and dynamic graph builders.
- `vocab`: serialized vocabulary used by the checkpoint.

These are data/model assets rather than editable project source files.

